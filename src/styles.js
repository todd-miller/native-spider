
import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    height: '100%',
    // color: "#000",
    backgroundColor: "#7A1B07"
  },
  text: {
    color: '#000'
  },
  ball: {
    borderRadius: 0,
  }
});
